spring:
  application:
   name: AUTH-SERVICE
  jpa:
    hibernate.ddl-auto: update
    show-sql: true
  datasource:
    url: jdbc:mysql://localhost:3306/usersecuritysqldb
    username: root
    password: root#123
    driver-class-name: com.mysql.cj.jdbc.Driver
  config:
    import: optional:configserver:http://localhost:9296

